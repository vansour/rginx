Config(
    runtime: RuntimeConfig(
        shutdown_timeout_secs: 10,
    ),
    // Main config owns global defaults only. Site bindings, certificates,
    // upstreams and routes live in `conf.d/*.ron`, matching nginx layout.
    server: ServerConfig(),
    upstreams: [],
    locations: [],
    servers: [
        // @include "conf.d/*.ron"
    ],
)
